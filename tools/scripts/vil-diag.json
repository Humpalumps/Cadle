[
  { "wait": 5.0 },
  { "hour": 11 },
  { "tp": [118, null, -96] },
  { "wait": 1.0 },
  { "eval": "Object.keys(__game.game.world).join(',')+' || GAME:'+Object.keys(__game.game).join(',')" },
  { "eval": "(()=>{const P=__game.game.world.props,T=__game.game.terrain||__game.game.world.terrain;const V=(m,i)=>[m.elements[i],m.elements[i+1],m.elements[i+2]];const out=[];const all=[];for(const v of P.villagers)all.push(v);for(const v of all){const m=v.mesh;m.updateWorldMatrix(true,true);const info={pos:[+m.position.x.toFixed(2),+m.position.y.toFixed(3),+m.position.z.toFixed(2)],baseY:v.baseY!==undefined?+v.baseY.toFixed(3):null,scl:+m.scale.x.toFixed(4),vis:m.visible,meshes:[],nbones:0,bonesample:[],feet:[]};info.tY=T?+T.heightAt(m.position.x,m.position.z).toFixed(3):null;m.traverse((o)=>{if(o.isMesh)info.meshes.push({n:o.name.slice(0,18),tris:Math.round((o.geometry.index?o.geometry.index.count:o.geometry.attributes.position.count)/3),vis:o.visible,fc:o.frustumCulled,bs:o.geometry.boundingSphere?+o.geometry.boundingSphere.radius.toFixed(2):null,nb:o.isSkinnedMesh&&o.skeleton?o.skeleton.bones.length:0});if(o.isBone){info.nbones++;if(info.bonesample.length<40)info.bonesample.push(o.name);if(/foot|ankle|toe|ball|heel|leg/i.test(o.name)){const e=o.matrixWorld.elements;info.feet.push({n:o.name,y:+e[13].toFixed(3),ax:V(o.matrixWorld,0).map((q)=>+q.toFixed(2)),ay:V(o.matrixWorld,4).map((q)=>+q.toFixed(2)),az:V(o.matrixWorld,8).map((q)=>+q.toFixed(2))});}}});out.push(info);}return JSON.stringify(out);})()" },
  { "eval": "(()=>{const P=__game.game.world.props;const w=P.wayfinders&&P.wayfinders[0];if(!w)return 'none';const m=w.mesh;m.updateWorldMatrix(true,true);const o=[];m.traverse((x)=>{if(x.isBone)o.push(x.name);});return 'wf bones '+o.length+': '+o.join(' ');})()" },
  { "eval": "(()=>{const C=__game.game.world.colliders||__game.game.colliders;const near=[];for(const c of C.items){let s=null;if(c.type==='box'){const b=c.box;const cx=(b.min.x+b.max.x)/2,cz=(b.min.z+b.max.z)/2;if((cx-118)**2+(cz+96)**2<3600)s='BOX ['+b.min.x.toFixed(1)+','+b.min.y.toFixed(1)+','+b.min.z.toFixed(1)+']-['+b.max.x.toFixed(1)+','+b.max.y.toFixed(1)+','+b.max.z.toFixed(1)+']'+(c.walkable?' WALK':'');}else if(c.type==='sphere'){if((c.pos.x-118)**2+(c.pos.z+96)**2<3600)s='SPH '+c.pos.x.toFixed(1)+','+c.pos.y.toFixed(1)+','+c.pos.z.toFixed(1)+' r'+c.r.toFixed(2);}else{if((c.a.x-118)**2+(c.a.z+96)**2<3600)s='CAP '+c.a.x.toFixed(1)+','+c.a.y.toFixed(1)+','+c.a.z.toFixed(1)+'->'+c.b.y.toFixed(1)+' r'+c.r.toFixed(2);}if(s)near.push(s);}return 'total='+C.items.size+' near60m='+near.length+'\\n'+near.join('\\n');})()" }
]
