[{"wait":25},{"hour":10},{"wait":3},
 {"eval":"(()=>{const G=window.__game;G.god(true);G.clearEnemies();G.passive(true);const p=G.game.player.position;G._s=G.spawn('skyserpent',p.x+38,p.z);return 'spawned';})()"},
 {"wait":3},
 {"eval":"(()=>{const G=window.__game,e=G._s,c=G.game.camera.position;const dx=e.position.x-c.x,dy=e.position.y-c.y,dz=e.position.z-c.z,h=Math.hypot(dx,dz);G._yaw=Math.atan2(-dx,-dz);G._p=Math.atan2(dy,h);G.look(G._yaw,G._p);return `d=${Math.hypot(h,dy).toFixed(1)} lod=${e._lod} yaw=${G._yaw.toFixed(2)} pitch=${G._p.toFixed(2)}`;})()"},
 {"wait":0.4},{"shot":"serp-pos"},
 {"eval":"(()=>{const G=window.__game;G.look(G._yaw,-G._p);return 'flipped';})()"},
 {"wait":0.4},{"shot":"serp-neg"},
 {"eval":"(()=>{const G=window.__game,e=G._s,c=G.game.camera.position;const dx=e.position.x-c.x,dy=e.position.y-c.y,dz=e.position.z-c.z,h=Math.hypot(dx,dz);G.look(Math.atan2(-dx,-dz),Math.atan2(dy,h));return `re-aimed d=${Math.hypot(h,dy).toFixed(1)} lod=${e._lod}`;})()"},
 {"burst":{"name":"serp-band","n":8,"interval":0.1}},
 {"eval":"(()=>{const G=window.__game,p=G.game.player.position;G.clearEnemies();G._d=G.spawn('drake',p.x+38,p.z);return 'drake';})()"},
 {"wait":3},
 {"eval":"(()=>{const G=window.__game,e=G._d,c=G.game.camera.position;const dx=e.position.x-c.x,dy=e.position.y-c.y,dz=e.position.z-c.z,h=Math.hypot(dx,dz);G.look(Math.atan2(-dx,-dz),Math.atan2(dy,h));return `drake d=${Math.hypot(h,dy).toFixed(1)} lod=${e._lod}`;})()"},
 {"burst":{"name":"drake-band","n":8,"interval":0.1}},
 {"eval":"(()=>{const G=window.__game,p=G.game.player.position;G.clearEnemies();G._s=G.spawn('skyserpent',p.x+16,p.z);return 'near';})()"},
 {"wait":3},
 {"eval":"(()=>{const G=window.__game,e=G._s,c=G.game.camera.position;const dx=e.position.x-c.x,dy=e.position.y-c.y,dz=e.position.z-c.z,h=Math.hypot(dx,dz);G.look(Math.atan2(-dx,-dz),Math.atan2(dy,h));return `near serpent d=${Math.hypot(h,dy).toFixed(1)} lod=${e._lod}`;})()"},
 {"wait":0.4},{"shot":"serp-near"},
 {"burst":{"name":"serp-near","n":8,"interval":0.35}}]
