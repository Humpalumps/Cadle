[{"wait":31},
 {"eval":"window.__game.god(true);window.__game.passive(true);window.__game.clearEnemies();'ok'"},
 {"wait":2},
 {"eval":"(()=>{const g=window.__game;const P=g.game.player.controller.position;const types=['hound','sentinel','drake','treant','wraith','wisp'];const out=[];const base=g.stats();out.push('BASE calls='+base.calls+' tris='+base.tris);for(const t of types){g.clearEnemies();g.spawn(t,P.x,P.z-8,{});const es=g.game.enemies?.alive||[];let meshes=0,mats=new Set(),tri=0,transp=0;for(const e of es){const r=e.root||e.group;r&&r.traverse(m=>{if(!m.isMesh&&!m.isInstancedMesh)return;meshes++;const mm=m.material;(Array.isArray(mm)?mm:[mm]).forEach(x=>{if(x){mats.add(x.uuid);if(x.transparent||x.alphaTest>0)transp++;}});const ge=m.geometry;if(ge){const n=ge.index?ge.index.count/3:(ge.attributes?.position?.count||0)/3;tri+=n*(m.isInstancedMesh?m.count:1);}});}out.push(t+' meshes='+meshes+' materials='+mats.size+' tris='+Math.round(tri)+' transparentOrAlphaTest='+transp);}g.clearEnemies();return out.join(' || ');})()"}]
