[
 {
  "wait": 3.0
 },
 {
  "eval": "new Promise(r=>{let n=0;const t=setInterval(()=>{n++;const g=window.__game&&window.__game.game;if((g&&g.enemies&&Object.keys(g.enemies.pools).length&&g.terrain)||n>300){clearInterval(t);r('ready@'+(n*0.2).toFixed(1)+'s');}},200);})"
 },
 {
  "wait": 2.0
 },
 {
  "hour": 11
 },
 {
  "eval": "const g=__game.game; __game.god(true); __game.clearEnemies(); g.enemies.passive=false; __game.teleport(0,null,0); __game.look(3.1416,0.02); const go=__game.spawn('warden',0,22,{level:5}); go.alert=true; go.lastSeenT=g.time; go.lastSeen.copy(g.player.position); go.home.copy(go.position); window.__ev=[]; g.events.on('enemy:attack',(d)=>{const e=d.enemy,p=g.player.position; window.__ev.push([+g.time.toFixed(2),e.type,d.kind,+Math.hypot(e.position.x-p.x,e.position.z-p.z).toFixed(1)]);}); window.__tr=[]; const step=()=>{const e=g.enemies.list.find(x=>x.type==='warden'); const p=g.player.position; if(e) window.__tr.push([+g.time.toFixed(2), +Math.hypot(e.position.x-p.x,e.position.z-p.z).toFixed(1), e.state, +Math.hypot(e.velocity.x,e.velocity.z).toFixed(2)]); requestAnimationFrame(step);}; step(); 'go'"
 },
 {
  "wait": 24.0
 },
 {
  "eval": "const tr=window.__tr, ev=window.__ev; const st={}; for(const r of tr) st[r[2]]=(st[r[2]]||0)+1; const sp=tr.filter(r=>r[2]==='chase').map(r=>r[3]).sort((a,b)=>a-b); const d=tr.map(r=>r[1]); JSON.stringify({secs:+(tr[tr.length-1][0]-tr[0][0]).toFixed(1), start:d[0], min:Math.min(...d), states:st, chaseSpeedMedian:sp[Math.floor(sp.length/2)], chaseSpeedP90:sp[Math.floor(sp.length*0.9)], dist:d.filter((_,i)=>i%Math.ceil(d.length/20)===0), attacks:ev})"
 }
]