diff --git a/src/player/Abilities.js b/src/player/Abilities.js
index 1e31014..c9d9fe7 100644
--- a/src/player/Abilities.js
+++ b/src/player/Abilities.js
@@ -115,10 +115,25 @@ function handGeo() {
   }
   const th = new THREE.CapsuleGeometry(0.012, 0.05, 3, 6); th.rotateX(-1.0); th.rotateZ(0.7); th.translate(-0.06, 0.005, -0.028); parts.push(th);
   const geo = mergeGeometries(parts);
-  const p = geo.attributes.position, cols = new Float32Array(p.count * 3);
+  // BAKED SHADING (wave-5 critique: "two enormous flat solid-yellow cartoon hands ... zero shading").
+  // MeshBasicMaterial is unlit, so the shading is baked into full-RGB vertex colours instead: a fixed key
+  // light (upper-left, toward camera) x an ember->gold->hot ramp wrist->fingertips, plus a view-rim term so
+  // grazing edges pick up a pale-gold fresnel fringe. The hand now reads as a modelled energy gauntlet with
+  // form — dark ember in the palm shadow, hot gold on lit knuckles/tips — while staying one unlit draw.
+  const p = geo.attributes.position, nr = geo.attributes.normal, cols = new Float32Array(p.count * 3);
+  const L = new THREE.Vector3(-0.35, 0.75, 0.55).normalize();
+  // Values kept DEEP on purpose: the weapons overlay renders inside the composer, so scene auto-exposure
+  // (high in a dark region) multiplies these — a bright gold here clips to the flat cream mitten the
+  // wave-5 critique flagged. Min channel low = the hue survives the boost (blob decree).
+  const deep = [0.50, 0.16, 0.02], gold = [0.95, 0.55, 0.14], hot = [1.0, 0.75, 0.30];
   for (let i = 0; i < p.count; i++) {
-    const t = Math.max(0, Math.min(1, (0.28 - p.getZ(i)) / 0.42)), b = 0.15 + 0.85 * t * t;
-    cols[i * 3] = cols[i * 3 + 1] = cols[i * 3 + 2] = b;
+    const t = Math.max(0, Math.min(1, (0.28 - p.getZ(i)) / 0.42));            // wrist 0 -> fingertip 1
+    const ndl = Math.max(0, nr.getX(i) * L.x + nr.getY(i) * L.y + nr.getZ(i) * L.z);
+    const rim = Math.pow(Math.max(0, 1 - Math.abs(nr.getZ(i))), 2.0) * 0.30;  // silhouette-edge glow
+    const k = (0.18 + 0.82 * ndl) * (0.35 + 0.65 * t * t);
+    const tip = Math.max(0, t * t * ndl - 0.15);
+    for (let ch = 0; ch < 3; ch++)
+      cols[i * 3 + ch] = Math.min(1, deep[ch] + (gold[ch] - deep[ch]) * k + (hot[ch] - gold[ch]) * tip + rim * hot[ch] * 0.35) * 0.9;
   }
   geo.setAttribute('color', new THREE.BufferAttribute(cols, 3));
   return geo;
@@ -165,7 +180,9 @@ export class Abilities {
     this.puffMesh = im(new THREE.SphereGeometry(0.07, 8, 6), 16);
     this.puffs = Array.from({ length: 16 }, () => ({ alive: false, t: 0, pos: new THREE.Vector3() }));
     // star motes orbiting the player while the super is up (bolts themselves are combat.projectile visuals)
-    this.motes = new THREE.InstancedMesh(new THREE.IcosahedronGeometry(0.13, 1), new THREE.MeshBasicMaterial({ ...ADD, color: new THREE.Color(COL.super) }), 10);
+    // motes: deep ember gold at reduced level — full COL.super on an unlit additive icosahedron rendered as
+    // FLAT opaque yellow low-poly discs (wave-5 "flat yellow disc"); deep hue + smaller reads as gold embers
+    this.motes = new THREE.InstancedMesh(new THREE.IcosahedronGeometry(0.09, 1), new THREE.MeshBasicMaterial({ ...ADD, color: new THREE.Color(GOLD_CORE).multiplyScalar(0.72) }), 10);
     this.motes.frustumCulled = false; this.motes.visible = false; this.motes.instanceMatrix.setUsage(THREE.DynamicDrawUsage); this.root.add(this.motes);
     // fallback super hand glows (world-space, camera-anchored) — used only when the weapons overlay scene isn't available
     const hm = new THREE.MeshBasicMaterial({ ...ADD, color: new THREE.Color(COL.super), opacity: 0.5 });
@@ -187,15 +204,22 @@ export class Abilities {
   _ensureVM() {
     if (this._vm) return true;
     const w = this.player.weapons; if (!w?.scene || !w?.cam) return false;
-    const gold = new THREE.Color(COL.super);
-    // saturated gradient core: vertexColors ramp * deep gold, brightness <= 1 so the hand keeps its hue at noon
-    const core = new THREE.MeshBasicMaterial({ color: GOLD_CORE, vertexColors: true, side: THREE.DoubleSide, fog: false });
-    const shell = new THREE.MeshBasicMaterial({ ...ADD, color: gold.clone().multiplyScalar(0.85), opacity: 0.35, side: THREE.DoubleSide });
+    // shaded gradient core: full-RGB baked-lighting vertex colours (see handGeo) — material color stays white
+    // so the bake IS the look; brightness <= 1 so the hand keeps its hue at noon
+    const core = new THREE.MeshBasicMaterial({ color: 0xffffff, vertexColors: true, side: THREE.DoubleSide, fog: false });
+    // fresnel-style rim: BackSide-only additive shell — visible only where it silhouettes past the core, so
+    // it reads as an energy rim, not the flat "lighter outline" milk wash of the old DoubleSide shell
+    const shellCol = new THREE.Color(0xff9a20).multiplyScalar(0.8);
+    // per-hand side: the left hand mirrors with group.scale.x = -1, which flips winding — BackSide there
+    // would render as the front and bury the core under additive milk. FrontSide on the mirrored hand is
+    // the same visual (far-side silhouette rim).
+    const shells = [new THREE.MeshBasicMaterial({ ...ADD, color: shellCol, opacity: 0.4, side: THREE.FrontSide }),
+                    new THREE.MeshBasicMaterial({ ...ADD, color: shellCol, opacity: 0.4, side: THREE.BackSide })];
     const geo = this._handGeo = handGeo();
     this._vm = { group: new THREE.Group(), hands: [], glows: [], wisps: [], vig: null };
     for (let i = 0; i < 2; i++) { // 0 = left, 1 = right
       const h = new THREE.Group();
-      const c = new THREE.Mesh(geo, core); const s = new THREE.Mesh(geo, shell); s.scale.setScalar(1.09); // tight shell keeps the finger silhouette readable
+      const c = new THREE.Mesh(geo, core); const s = new THREE.Mesh(geo, shells[i]); s.scale.setScalar(1.06); // tight shell keeps the finger silhouette readable
       const glow = new THREE.Sprite(new THREE.SpriteMaterial({ ...ADD, map: this._glow, color: new THREE.Color(GOLD_CORE), opacity: 0.4 }));
       glow.scale.set(0.28, 0.28, 1); glow.position.set(0, 0.03, -0.055);
       h.add(c, s, glow);
@@ -508,10 +532,12 @@ export class Abilities {
     if (this._ensureVM()) { this._vm.group.visible = true; this._vmT = 0; this._pulse[0] = this._pulse[1] = 1; if (this._mv) { this._mv.t = -1; this._mv.group.visible = false; } }
     else for (const h of this.hands) h.visible = true; // fallback: world-space hand glows
     const view = this.player.view; if (view && typeof view.baseFov === 'number') { this._fovDelta = 6; view.baseFov += this._fovDelta; }
-    postfx?.flash?.(c, 0.9, 0.5); postfx?.kick?.(1.0); view.shake?.(1.4, 0.5);
-    const p = this.player.position; this._burst(this._v1.set(p.x, p.y + 1, p.z), c, 3, 0.5); this._burst(this._v1, GOLD_CORE, 1.4, 0.2); this._ring(p, c, 9, 0.7); this._ring(p, GOLD_CORE, 5, 0.4);
+    // kick <= 0.3 (was 1.0): the CA pulse at full strength is a screen-wide barrel warp that smears
+    // violet/red fringes across the frame edges — the super pop is carried by the flash + shake + burst
+    postfx?.flash?.(c, 0.55, 0.4); postfx?.kick?.(0.3); view.shake?.(1.4, 0.5);
+    const p = this.player.position; this._burst(this._v1.set(p.x, p.y + 1, p.z), c, 1.6, 0.4); this._burst(this._v1, GOLD_CORE, 0.9, 0.2); this._ring(p, c, 9, 0.7); this._ring(p, GOLD_CORE, 5, 0.4);
     this._sigil('aura', p, c, 2.2, 6, true);
-    vfx?.emit?.('aether-burst', this._v1, { color: c, count: 80, scale: 1 }); vfx?.flash?.(this._v1, { color: c, intensity: 12, distance: 20, duration: 0.25 });
+    vfx?.emit?.('aether-burst', this._v1, { color: c, count: 55, scale: 1 }); vfx?.flash?.(this._v1, { color: c, intensity: 12, distance: 20, duration: 0.25 });
     audio?.play?.('ability-super'); hud?.notify?.('STARFALL', 'Aetherweaver Super');
   }
   _endSuper() {
diff --git a/src/vfx/Particles.js b/src/vfx/Particles.js
index baae91f..b80524a 100644
--- a/src/vfx/Particles.js
+++ b/src/vfx/Particles.js
@@ -49,7 +49,7 @@ const VERT = /* glsl */`
     // to sit 0.5 m from the eye and fill the whole frame. Fade by projected COVERAGE (size/depth): a quad
     // spanning ~2/3 of the frame starts fading, past ~1.3 frames it is gone. Muzzle sprites (~0.3 m at the
     // barrel) stay under the threshold; a 3 m fireball 10 m away is untouched.
-    env *= 1.0 - smoothstep(1.2, 2.4, size / max(depth, 0.05));
+    env *= 1.0 - smoothstep(0.95, 1.9, size / max(depth, 0.05));   // tightened from (1.2,2.4): a quad ~2/3 of the frame now fades, gone at ~1.9 frames — near-lens detonation sheets were still washing pale ground (gate vfx6); muzzle sprites ship at scale 0.15 (~0.3 m at the barrel, ratio ~0.4) and are untouched
     vFog = 1.0 - exp(-fogDensity * fogDensity * depth * depth);
     vCol = vec4(mix(iCol0, iCol1, t), iAR.x * env);
     float tex = iTS.x;
diff --git a/src/vfx/Textures.js b/src/vfx/Textures.js
index abe220a..8be5a80 100644
--- a/src/vfx/Textures.js
+++ b/src/vfx/Textures.js
@@ -4,7 +4,7 @@ import { fbm, mulberry32 } from '../core/Noise.js';
 /**
  * Procedural canvas textures for VFX. All grayscale masks (rgb = a = intensity), colored in shaders.
  *   makeAtlas()  -> { texture, cols:4, rows:3 }  tiles: 0 glow, 1 spark, 2 smoke, 3 ring, 4 star, 5 rune, 6 dirt, 7 flare, 8 smoke2
- *   makeDecals() -> 2x1 atlas: 0 bullet hole, 1 scorch
+ *   makeDecals() -> 4x1 atlas: 0 bullet pit (dark), 1 scorch, 2 bullet rim (light, tint > 1 brightens), 3 spare
  *   makeSigil()  -> 512px rune circle (sigil/heal/levelup ground glyphs)
  */
 export const TEX = { GLOW: 0, SPARK: 1, SMOKE: 2, RING: 3, STAR: 4, RUNE: 5, DIRT: 6, FLARE: 7, SMOKE2: 8 };
@@ -95,24 +95,40 @@ export function makeAtlas(seed = 1) {
 }
 
 export function makeDecals(seed = 1) {
-  const c = canvas(T * 2, T), ctx = c.getContext('2d');
+  const c = canvas(T * 4, T), ctx = c.getContext('2d');
   const rnd = mulberry32(seed + 313);
-  // 0 bullet hole: dark pit, chipped rim, radial cracks
-  { const cracks = []; for (let k = 0; k < 7; k++) cracks.push([rnd() * 6.283, 0.3 + rnd() * 0.45, 0.03 + rnd() * 0.03]);
+  // 0 bullet hole PIT. Rebuilt (wave-5 critique: "a huge pure-black soft radial blob ... no crater, no rim"):
+  // the old tile was a full-tile soft blob — multiplied down to 0.28 it read as a hole in the world. Now the
+  // DARK part is only the small centre pit (r < ~0.3 of the tile) plus thin radial fracture cracks; the outer
+  // area of the tile is near-zero so the multiply decal darkens almost nothing outside the crater itself.
+  { const cracks = []; for (let k = 0; k < 6; k++) cracks.push([rnd() * 6.283, 0.34 + rnd() * 0.34, 0.022 + rnd() * 0.02]);
     fillTile(ctx, 0, 0, (x, y, r) => {
       const n = fbm(x * 5, y * 5, { octaves: 3, seed: seed + 5 }) * 0.5 + 0.5;
-      let m = sstep(0.3 + 0.16 * (n - 0.5), 0.12, r) + 0.5 * sstep(0.55, 0.22, r + 0.3 * (n - 0.5));
+      let m = sstep(0.30 + 0.10 * (n - 0.5), 0.10, r)               // hard little pit
+            + 0.35 * sstep(0.42, 0.24, r + 0.2 * (n - 0.5));        // narrow charred lip, fades fast
       const ang = Math.atan2(y, x);
-      for (const [a, len, w] of cracks) { const da = Math.abs(((ang - a + Math.PI * 3) % (Math.PI * 2)) - Math.PI); const d = da * r; if (r < len) m = Math.max(m, 0.9 * sstep(w * 1.6, 0, d) * (1 - r / len) * (0.6 + 0.8 * n)); }
+      for (const [a, len, w] of cracks) { const da = Math.abs(((ang - a + Math.PI * 3) % (Math.PI * 2)) - Math.PI); const d = da * r; if (r < len) m = Math.max(m, 0.8 * sstep(w * 1.5, 0, d) * (1 - r / len) * (0.6 + 0.8 * n)); }
       return m;
     }); }
+  // 2 bullet hole RIM: the LIGHT half of the crater — chipped fresh-fracture ring around the pit plus short
+  // debris streaks. Drawn as its own decal with a >1 tint (multiply blending brightens where tint > 1), so
+  // the hole reads as crater + pale fractured lip instead of an unlit black dot. Alpha modest: a hint, not paint.
+  { const chips = []; for (let k = 0; k < 9; k++) chips.push([rnd() * 6.283, 0.55 + rnd() * 0.5]);
+    fillTile(ctx, 2, 0, (x, y, r) => {
+      const n = fbm(x * 6 + 3, y * 6 - 1, { octaves: 3, seed: seed + 15 }) * 0.5 + 0.5;
+      const d0 = (r - (0.36 + 0.10 * (n - 0.5))) / 0.10;
+      let m = 0.75 * Math.exp(-d0 * d0) * (0.5 + 0.5 * n);          // ragged bright ring just outside the pit
+      const ang = Math.atan2(y, x);
+      for (const [a, amp] of chips) { const da = Math.abs(((ang - a + Math.PI * 3) % (Math.PI * 2)) - Math.PI); m += 0.45 * amp * sstep(0.22, 0, da) * sstep(0.72, 0.34, r) * sstep(0.3, 0.42, r) * n; }
+      return Math.min(1, m) * sstep(0.9, 0.6, r);
+    }); }
   // 1 scorch: fbm blotch with streaks
   fillTile(ctx, 1, 0, (x, y, r) => {
     const n = fbm(x * 3 + 1, y * 3 - 2, { octaves: 5, seed: seed + 8 }) * 0.5 + 0.5;
     const ang = Math.atan2(y, x); const streak = 0.5 + 0.5 * Math.sin(ang * 9 + n * 6);
     return Math.min(1, sstep(0.98, 0.25, r + 0.4 * (n - 0.5) + 0.12 * streak) * (0.55 + 0.45 * n) * sstep(0.9, 0.0, r * 0.4) + 0.6 * sstep(0.5, 0.0, r));
   });
-  return { texture: tex(c), cols: 2, rows: 1 };
+  return { texture: tex(c), cols: 4, rows: 1 };
 }
 
 export function makeSigil(seed = 1) {
diff --git a/src/vfx/VFX.js b/src/vfx/VFX.js
index 9cba59e..b7fd166 100644
--- a/src/vfx/VFX.js
+++ b/src/vfx/VFX.js
@@ -400,7 +400,14 @@ export class VFX {
     const type = o.type ?? 'bullet';
     if (type === 'sigil') return this.sigil(p, { normal: n, color: o.color, size: o.size ?? 2.5, duration: o.duration ?? 10 });
     if (type === 'scorch') this.decals.add(p, n, o.size ?? 2.5, 1, o.life ?? 60, 0.12, 0.1, 0.09, o.rot);
-    else this.decals.add(p, n, o.size ?? 0.16, 0, o.life ?? 45, 0.28, 0.26, 0.24, o.rot);
+    else {
+      // bullet hole = crater PAIR (wave-5: one big soft multiply blob read as "a hole in the world"):
+      // small dark pit + a wider light fractured rim (tint > 1 = multiply-brighten), same random rotation
+      // so the chips line up with the cracks. Sized honestly: ~0.1-0.2 m holes.
+      const sz = Math.min(o.size ?? 0.14, 0.2), rot = o.rot ?? Math.random() * 6.283;
+      this.decals.add(p, n, sz, 0, o.life ?? 45, 0.3, 0.28, 0.26, rot);
+      this.decals.add(p, n, sz * 1.9, 2, o.life ?? 45, 1.32, 1.28, 1.22, rot);
+    }
   }
   flash(p, o = NOPTS) {
     let f = null; for (const x of this.lights) if (x.t >= x.dur) { f = x; break; }
@@ -480,7 +487,7 @@ export class VFX {
       const n = e.normal ?? this._up, s = e.surface;
       const preset = s === 'water' ? 'impact-water' : (s === 'prop' || s === 'rock') ? 'impact-rock' : 'impact-terrain';
       this._emit(preset, e.point, { normal: n, element: e.element });
-      if (s !== 'water') this.decal(e.point, n, { type: 'bullet', size: 0.16 + Math.random() * 0.1 }); // wave-1 feel audit: 0.12-0.2 m decals read as thin — one notch bigger
+      if (s !== 'water') this.decal(e.point, n, { type: 'bullet', size: 0.11 + Math.random() * 0.07 }); // 0.11-0.18 m: honest bullet-hole scale (wave-5: bigger read as a black void); the light rim decal is what keeps it readable
     });
     ev.on('combat:explosion', (e) => {
       if (!e?.point) return;
@@ -578,7 +585,7 @@ const PRESETS = {
     // ~375 live additive particles this way. Thin with range; saturated hue + low hdr carries the read.
     const cp = v.game.camera.position, dd = Math.hypot(p.x - cp.x, p.y - cp.y, p.z - cp.z);
     const near = 1 - 0.72 * Math.min(1, Math.max(0, (dd - 22) / 40));
-    const sat = _c2.copy(c).offsetHSL(0, 0.35, -0.05);
+    const sat = _c2.copy(c).offsetHSL(0, 0.5, -0.12);   // deeper than the old (0.35,-0.05): volley trails STACK — Combat attaches this to every bolt — and a pale start summed over pale ground was the celestial/sunken cyan mist wash
     b.reset(v.add, p).jitter(0.02 * s).spread(3.14).speed(0, 0.3).life(0.18, 0.3).size(0.09 * s, 0.14 * s, 0.15).tex(TEX.GLOW).color(sat, sat).hdr(1.4 * near, 1.0 * near).alpha(0.5 * near).fade(0, 0.25).burst(1);   // alpha 0.7->0.5: the mist over the bolt's own core sphere is what bleached it to a pale egg (gate r3, vale)
     b.reset(v.add, p).axis(o.dir ?? UP).spread(0.35).speed(0.5, 2.2).life(0.2, 0.4).size(0.02 * s, 0.04 * s, 0.2).tex(TEX.SPARK).color(sat, sat).hdr(1.6 * near, 1.2 * near).stretch(0.03).gravity(3).drag(1).fade(0, 0.5).burst(1 + k);
   },
@@ -647,7 +654,7 @@ const PRESETS = {
     b.reset(v.alpha, p).jitter(0.09 * s).spread(3.14).speed(0.3, 0.9).life(0.45, 0.7).size(0.4 * s, 0.6 * s, 2.2).tex(TEX.SMOKE).color(0x120a1e).vary(0.3).alpha(0.78).rot().spin(2).drag(2).fade(0.05, 0.35).burst(5 * k);
     b.reset(v.add, p).tex(TEX.GLOW).size(0.85 * s, 1.15 * s, 1.5).life(0.3).color(sat, sat).hdr(1.5 + 0.5 * day, 0.5).alpha(0.95).fade(0, 0.28).burst(1); // big saturated colored halo = THE element read
     b.reset(v.add, p).tex(TEX.GLOW).size(0.4 * s, 0.55 * s, 1.6).life(0.2).color(sat, sat).hdr(2.6 + 1.0 * day, 1.1).alpha(0.9).fade(0, 0.3).burst(1);
-    b.reset(v.add, p).tex(TEX.STAR).size(0.26 * s, 0.34 * s, 1.4).life(0.13).color(sat, c).hdr(3.5 + 1.5 * day, 2).rot().fade(0, 0.3).burst(1);           // small hot core only, in the saturated hue (was white at hdr 9: the lost-region "near-white egg over the golem chest")
+    b.reset(v.add, p).tex(TEX.STAR).size(0.26 * s, 0.34 * s, 1.4).life(0.13).color(sat, sat).hdr(3.5 + 1.5 * day, 2).rot().fade(0, 0.3).burst(1);           // small hot core only, in the saturated hue both ends (was white at hdr 9: the lost-region "near-white egg over the golem chest")
     b.reset(v.add, p).axis(n).spread(1.1).speed(4 * s, 11 * s).life(0.28, 0.6).size(0.04, 0.075, 0.3).tex(TEX.SPARK).color(sat, sat).hdr(4.5 + 1.5 * day, 2).stretch(0.06).gravity(7).drag(2.5).fade(0, 0.5).burst(16 * k);
     // ember flecks: brief saturated chunks that arc out and die fast — the physical "something broke off" presence the
     // wave-1 feel audit asked for. Saturated colour at moderate HDR (blob law: hue survives ACES, value never clips).
@@ -667,7 +674,7 @@ const PRESETS = {
     // Elemental explosions take the ELEMENT_COLORS pastel (arc/stasis 0x*d8ff, strand 0x7cff9c) — as a tracer
     // tint that is fine, but as the BODY colour of a whole fireball a pastel IS near-white: stasis/arc/strand
     // detonations were flagged as cream clouds in tundra/celestial/shadowfen. Deep-saturate it first.
-    const fire = o.element === 'arc' || o.element === 'void' || o.element === 'stasis' || o.element === 'strand' ? _c2.copy(c).offsetHSL(0, 0.55, -0.14) : _c2.set(0xff5a18);
+    const fire = o.element === 'arc' || o.element === 'void' || o.element === 'stasis' || o.element === 'strand' ? _c2.copy(c).offsetHSL(0, 0.6, -0.18) : _c2.set(0xff5a18);
     const fl = n ? p.y - 0.02 : -1e9;
     // Blob decree re-author (wave-5: enemy bolts detonate AT the camera and this preset washed 8 of 10
     // regions near-white): every near-white literal is gone — the punch is a SATURATED fire hue at hdr low
@@ -681,17 +688,24 @@ const PRESETS = {
     // pale ground (snow, marble, sand) and lifts an already-bright surface into the detector's cream band —
     // combat gate r4's tundra/celestial residuals were exactly this, not a clipped element.
     const er = Math.min(1, 2.2 / Math.max(1e-3, r));
+    // Near-lens FOOTPRINT clamp (combat gate residuals, celestial/shadowfen): a detonation 4-8 m out grows
+    // its ring to ~3.8r and its flare to ~1.7r — over pale ground (marble, snow) that additive sheet covers
+    // half the frame and lifts an already-bright surface into the cream band even at a fully saturated hue.
+    // The coverage fade in Particles.js only kills quads that fill the WHOLE frame. So the big soft layers
+    // (halo/flare/ring) spawn SMALLER when the blast is close: at 12+ m full size, at 4 m ~a third. Punch at
+    // the lens comes from the hot core + sparks + shake/CA, not from a screen-wide disc.
+    const kn = Math.min(1, Math.max(0.35, dd / 12));
     b.reset(A, p).tex(TEX.GLOW).size(0.38 * r, 0.5 * r, 1.9).life(0.16).color(fire, fire).hdr(3 * fr, 1.5 * fr).fade(0, 0.25).burst(1);                // hot core in the FIRE hue itself: even HOT_TINTed white lands at channel-spread ~30 once capped, which is exactly the detector's cream band
-    b.reset(A, p).tex(TEX.GLOW).size(1.1 * r, 1.3 * r, 1.5).life(0.28).color(fire, 0x7a1800).hdr(1.2 * fr * er, 0.3).alpha(0.85).fade(0, 0.25).burst(1);        // warm halo (er: per-pixel energy falls as the disc grows)
-    b.reset(A, p).tex(TEX.FLARE).size(1.4 * r, 1.7 * r, 1.5).life(0.11).color(fire, 0x8a2000).hdr(2.2 * fr * er, 1).rot().fade(0, 0.2).burst(1);           // flare petals in the element hue, not cream-white
-    b.reset(A, p).jitter(0.3 * r).spread(3.14).speed(1.6 * r, 4 * r).life(0.5, 1.0).size(0.18 * r, 0.36 * r, 2.6).tex(TEX.SMOKE).color(fire, 0x8a1a00).hdr(1.2 * fr, 0.25).vary(0.5).alpha(0.85).rot().spin(3).drag(3.2).gravity(-2.5).fade(0.0, 0.3).burst(Math.max(4, Math.round(12 * k * fr))); // fire tongues (12 not 22 at hdr 1.2: a stack of tongues clips its BLUE channel first and the whole pile goes cream — fewer, dimmer, still fire)
+    b.reset(A, p).tex(TEX.GLOW).size(1.1 * r * kn, 1.3 * r * kn, 1.5).life(0.28).color(fire, 0x7a1800).hdr(1.2 * fr * er * (0.6 + 0.4 * kn), 0.3).alpha(0.85).fade(0, 0.25).burst(1);        // warm halo (er: per-pixel energy falls as the disc grows; kn also cuts ENERGY at the lens, not just size)
+    b.reset(A, p).tex(TEX.FLARE).size(1.4 * r * kn, 1.7 * r * kn, 1.5).life(0.11).color(fire, 0x8a2000).hdr(2.2 * fr * er * (0.6 + 0.4 * kn), 1).rot().fade(0, 0.2).burst(1);           // flare petals in the element hue, not cream-white
+    b.reset(A, p).jitter(0.3 * r).spread(3.14).speed(1.6 * r, 4 * r).life(0.5, 1.0).size(0.18 * r * (0.6 + 0.4 * kn), 0.36 * r * (0.6 + 0.4 * kn), 2.6).tex(TEX.SMOKE).color(fire, 0x8a1a00).hdr(1.2 * fr * (0.55 + 0.45 * kn), 0.25).vary(0.5).alpha(0.85).rot().spin(3).drag(3.2).gravity(-2.5).fade(0.0, 0.3).burst(Math.max(4, Math.round(12 * k * fr))); // fire tongues (12 not 22 at hdr 1.2: a stack of tongues clips its BLUE channel first and the whole pile goes cream — fewer, dimmer, still fire; kn: at the lens the stacked tongues were the shadowfen pale-green floor wash)
     // smoke ball: Destiny grenade gradient — fire-lit orange-brown -> mid grey-brown -> dark. Starts SMALL and semi-transparent
     // and grows (endMul 3.4), so 0.2-1.0 s reads as a churning fireball, not an instant ink blob.
     b.reset(AL, p).jitter(0.25 * r).spread(3.14).speed(0.35 * r, 0.85 * r).life(2.2, 3.4).size(0.26 * r, 0.42 * r, 3.4).tex(TEX.SMOKE).color(0xb06a2e, 0x272119).hdr(1.7, 1).lit(L(v)).vary(0.35).alpha(0.62).rot().spin(0.6).drag(1.0).gravity(-0.7).fade(0.16, 0.55).burst(14 * k);
     v.attach('exp-smoke', v._anchor(p), { duration: 1.8, rate: Math.max(6, 11 * v.mult), scale: r / 3 });                                              // smoke column keeps pumping from the site
     _v3.set(p.x, p.y + (n ? 0.95 : 0), p.z);                                                                                                          // grounded ring lifts clear of 1 m meadow grass
     if (n) b.reset(A, _v3).axis(n).flat(); else b.reset(A, _v3).rot();
-    b.tex(TEX.RING).size(0.42 * r, 0.42 * r, 9).life(0.55).color(fire, c).hdr(3 * fr * er, 1.5 * fr * er).alpha(0.95).fade(0, 0.18).burst(1);                              // shockwave — SATURATED start: this ring grows to ~4r and a detonation at the lens fills the frame bottom with it; a white start (even min-channel-capped) is a large near-white sheet
+    b.tex(TEX.RING).size(0.42 * r * kn, 0.42 * r * kn, 9).life(0.55).color(fire, fire).hdr(3 * fr * er, 1.5 * fr * er).alpha(0.95).fade(0, 0.18).burst(1);   // END colour stays the deep fire hue too: ending on the pastel ELEMENT_COLORS teal/cyan is what left a pale ring on white marble (cel-c7)                              // shockwave — SATURATED start: this ring grows to ~4r and a detonation at the lens fills the frame bottom with it; a white start (even min-channel-capped) is a large near-white sheet
     if (n) { _v3.set(p.x, p.y + 1.05, p.z);                                                                                                            // dust ring rides above the grass tops
       b.reset(AL, _v3).axis(n).ring(0.35 * r, 0.6 * r, 0.2).speed(1.3, 2.4).life(2.4, 3.8).size(0.3 * r, 0.5 * r, 2.4).tex(TEX.SMOKE).color(0x9c8760, 0x7a6a4e).lit(L(v)).vary(0.3).alpha(0.55).rot().spin(0.8).drag(1.0).gravity(-0.15).fade(0.05, 0.6).burst(14 * k);   // lingering TAN dust ring: light against green grass, dark against sky
       _v3.set(p.x, p.y + 0.9, p.z);                                                                                                                    // scorch "haze" discs: the decal itself is buried under 1 m grass, this is the visible char
@@ -720,19 +734,19 @@ const PRESETS = {
   death(v, p, o, k, s, c) {
     // the kill reward: dark void veil first, then a white-hot pop + double ring + rising motes — most readable vfx in the game
     const b = v.brush, A = v.add, day = v.day;
-    b.reset(v.alpha, p).jitter(0.35 * s).spread(3.14).speed(0.4, 1.2).life(1.5, 2.4).size(0.35 * s, 0.6 * s, 2.4).tex(TEX.SMOKE).color(0x0e0817, 0x1a1026).vary(0.3).alpha(0.72).rot().spin(1).drag(2).gravity(-0.6).fade(0.04, 0.45).burst(10 * k);
-    const dsat0 = _c3.copy(c).offsetHSL(0, 0.4, -0.06);   // rings grow LARGE (2.7 m): a white/pale start reads as a near-white sheet at melee — saturate; the small star/glow pops keep the white-hot identity
+    b.reset(v.alpha, p).jitter(0.4 * s).spread(3.14).speed(0.4, 1.2).life(1.5, 2.4).size(0.45 * s, 0.7 * s, 2.2).tex(TEX.SMOKE).color(0x0e0817, 0x1a1026).vary(0.3).alpha(0.8).rot().spin(1).drag(2).gravity(-0.6).fade(0.04, 0.45).burst(10 * k);   // veil bigger + denser: the dark backing is what lets the hue pop read on WHITE marble/snow without lifting it to cream
+    const dsat0 = _c3.copy(c).offsetHSL(0, 0.5, -0.14);   // rings grow LARGE (2.7 m): a white/pale start reads as a near-white sheet at melee — saturate + deepen; the small star/glow pops keep the white-hot identity
     // pop in the creature's own hue, not white: at hdr 5-7 the HOT_TINTed white still landed as a pale
     // ball post-cap (combat gate r3: tundra 22k-px cluster at rgb 232,234,233 was exactly this pop over
     // a dead wisp). The punch is the SIZE + double ring + flash; the colour is the element.
-    b.reset(A, p).tex(TEX.GLOW).size(0.42 * s, 0.55 * s, 2.2).life(0.28).color(dsat0, c).hdr(3, 1.5).fade(0, 0.3).burst(1);
-    b.reset(A, p).tex(TEX.STAR).size(0.55 * s, 0.7 * s, 2).life(0.24).color(dsat0, c).hdr(3.5, 1.8).rot().fade(0, 0.3).burst(1);
-    b.reset(A, p).axisUp().flat().tex(TEX.RING).size(0.3 * s, 0.3 * s, 9).life(0.55).color(dsat0, c).hdr(3.5, 1.5).alpha(0.9).fade(0, 0.2).burst(1);
-    b.reset(A, p).axisUp().flat().tex(TEX.RING).size(0.15 * s, 0.15 * s, 12).life(0.75).color(dsat0).hdr(3, 1.2).alpha(0.7).fade(0.1, 0.3).burst(1);
+    b.reset(A, p).tex(TEX.GLOW).size(0.42 * s, 0.55 * s, 1.8).life(0.28).color(dsat0, dsat0).hdr(2.2, 1.2).alpha(0.85).fade(0, 0.3).burst(1);   // pop glow: deep hue both ends, hdr down — the pale-cyan disc over white marble at a seraph death (gate vfx5, cel-hit-0)
+    b.reset(A, p).tex(TEX.STAR).size(0.55 * s, 0.7 * s, 2).life(0.24).color(dsat0, dsat0).hdr(2.6, 1.4).rot().fade(0, 0.3).burst(1);
+    b.reset(A, p).axisUp().flat().tex(TEX.RING).size(0.3 * s, 0.3 * s, 9).life(0.55).color(dsat0, dsat0).hdr(2.6, 1.2).alpha(0.9).fade(0, 0.2).burst(1);
+    b.reset(A, p).axisUp().flat().tex(TEX.RING).size(0.15 * s, 0.15 * s, 12).life(0.75).color(dsat0).hdr(2.2, 1).alpha(0.7).fade(0.1, 0.3).burst(1);
     // afterglow motes: at 8 m the old 0.11 m stars were sub-pixel and the kill tail vanished by 0.75 s. 2x size, longer life,
     // saturated at moderate HDR so the element colour holds through noon exposure — the payoff now reads for ~2.5 s.
-    const dsat = _c2.copy(c).offsetHSL(0, 0.35, 0);
-    b.reset(A, p).jitter(0.5 * s).axisUp().spread(0.5).speed(1.0, 2.8).life(1.8, 3.0).size(0.22 * s, 0.36 * s, 0.4).tex(TEX.STAR).color(dsat, dsat).hdr(3.6 + 1.6 * day, 1.8).rot().spin(3).swirl(1.5, 3, true).gravity(-1.5).drag(0.8).fade(0.08, 0.6).burst(36 * k);
+    const dsat = _c2.copy(c).offsetHSL(0, 0.5, -0.08);   // pastel elements (stasis 0x9fd8ff): +sat alone left the motes near-white at hdr 5 — deepen too, the hue is the payoff
+    b.reset(A, p).jitter(0.5 * s).axisUp().spread(0.5).speed(1.0, 2.8).life(1.8, 3.0).size(0.22 * s, 0.36 * s, 0.4).tex(TEX.STAR).color(dsat, dsat).hdr(3.2 + 1.2 * day, 1.8).rot().spin(3).swirl(1.5, 3, true).gravity(-1.5).drag(0.8).fade(0.08, 0.6).burst(36 * k);
     b.reset(A, p).jitter(0.5 * s).axisUp().spread(0.7).speed(0.5, 1.6).life(1.6, 2.6).size(0.42 * s, 0.75 * s, 0.4).tex(TEX.GLOW).color(dsat, dsat).hdr(1.7 + 0.5 * day).alpha(0.7).gravity(-0.8).drag(1).fade(0.1, 0.55).burst(14 * k);
     b.reset(A, p).jitter(0.3 * s).spread(3.14).speed(3, 7).life(0.4, 0.8).size(0.02, 0.04, 0.3).tex(TEX.SPARK).color(dsat0, c).hdr(4, 2).stretch(0.03).gravity(8).drag(2).fade(0, 0.5).burst(16 * k);
     v.flash(p, { color: c, intensity: 2 * s, distance: 10, duration: 0.4 });
@@ -783,13 +797,14 @@ const PRESETS = {
   flash(v, p, o, k, s, c) {
     // a single big GLOW at hdr 4 became a structureless white orb at night (night exposure is ~2x): cap HDR by daylight and
     // give it structure — saturated halo + small hot star core + a ring edge, so it reads as a spark pop, not a render error.
-    const b = v.brush, A = v.add, e = 0.45 + 0.55 * v.day, dur = o.duration ?? 0.15, sat = _c2.copy(c).offsetHSL(0, 0.35, -0.05);
+    const b = v.brush, A = v.add, e = 0.45 + 0.55 * v.day, dur = o.duration ?? 0.15, sat = _c2.copy(c).offsetHSL(0, 0.5, -0.12);
     b.reset(A, p).tex(TEX.GLOW).size(0.42 * s, 0.62 * s, 1.8).life(dur * 1.4).color(sat, sat).hdr(1.5 * e, 0.5 * e).alpha(0.9).fade(0, 0.25).burst(1);
-    b.reset(A, p).tex(TEX.STAR).size(0.2 * s, 0.26 * s, 1.6).life(dur).color(0xffffff, c).hdr(5 * e, 2 * e).rot().fade(0, 0.3).burst(1);
+    b.reset(A, p).tex(TEX.STAR).size(0.2 * s, 0.26 * s, 1.6).life(dur).color(sat, sat).hdr(3.5 * e, 1.6 * e).rot().fade(0, 0.3).burst(1);   // was white-start hdr 5: enemy telegraph pops in tundra rendered as pure-white stars over the flash-lit giant (gate vfx5)
     b.reset(A, p).rot().tex(TEX.RING).size(0.18 * s, 0.18 * s, 5).life(dur * 2).color(sat, sat).hdr(2.2 * e, 0.8 * e).alpha(0.85).fade(0, 0.2).burst(1);
   },
   aura(v, p, o, k, s, c) {
-    v.brush.reset(v.add, p).ring(0.45 * s, 0.6 * s, 0, 1).speed(1.5 * s).swirl(2.5 / s).life(0.8, 1.4).size(0.04 * s, 0.08 * s, 0.3).tex(TEX.STAR).color(0xffffff, c).hdr(2.5, 1.5).rot().spin(3).gravity(-0.4).fade(0.15, 0.5).burst(Math.max(1, k));
+    const sat = _c2.copy(c).offsetHSL(0, 0.45, -0.08);   // white-start motes on a pale-element creature read as white specks (blob law)
+    v.brush.reset(v.add, p).ring(0.45 * s, 0.6 * s, 0, 1).speed(1.5 * s).swirl(2.5 / s).life(0.8, 1.4).size(0.04 * s, 0.08 * s, 0.3).tex(TEX.STAR).color(sat, c).hdr(2.5, 1.5).rot().spin(3).gravity(-0.4).fade(0.15, 0.5).burst(Math.max(1, k));
   },
   charge(v, p, o, k, s, c) {
     v.brush.reset(v.add, p).ring(0.7 * s, 1.1 * s, 0, 0).speed(-2.2 * s, -2.8 * s).life(0.35, 0.4).size(0.03 * s, 0.07 * s, 0.2).tex(TEX.SPARK).color(c, c).hdr(2, 3.2).stretch(0.04).fade(0.2, 0.7).burst(Math.max(1, k));   // converge brightens via hdr, not toward white (telegraph windups fill the frame with these)
