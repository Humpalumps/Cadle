export const meta = {
  name: 'wave6-redo-3lanes',
  description: 'Redo the three spend-limit-killed lanes: enemies mixer (eye-judged), props ornament closures, vfx polish + combat gate to zero',
  phases: [{ title: 'Build', detail: '3 file-owned lanes in parallel' }],
}
const ROOT = 'C:/Users/ianca/Desktop/fps4/.claude/worktrees/cadle-character-load-perf-ee5b7b'
const COMMON = `Repo root: ${ROOT} (git worktree, branch claude/session-e5730b). Dev server ALREADY RUNNING at http://127.0.0.1:5179/ — never start another, never touch port 5173. You are a BUILDER on Cadle (three r185 + postprocessing; Destiny 2 feel × FF14 look; painterly-realistic, saturated-but-soft, warm golds / deep blue-violets, gold filigree on dark, luminous blue-violet aether). CLAUDE.md's ownership table binds you — edit ONLY your lane's files. Never touch git, tools/, progress.html.

CONTEXT: a previous run of your exact lane was killed mid-work by a spend limit; its half-finished diff was REVERTED and archived as a patch (path given in your lane brief) — you may consult it for ideas but START CLEAN from this brief; do not blindly apply it (it shipped user-visible breakage).

DISCIPLINE (2 other lanes run in this worktree simultaneously): Vite hot-reloads on every save — a harness run dying with "Execution context was destroyed" just needs a re-run. GPU: node tools/inspect.mjs WITHOUT --nolock (the lock serializes across lanes; expect waits); --nolock only for a single quick shot; NEVER run tools/gate.mjs. BLOB DECREE: any emissive/additive element that tone-maps to WHITE instead of its hue is a bug — saturate colour, cap intensity; hue survives ACES iff its SMALLEST channel stays under clip (see Brush.js BRUSH_MINCH_CAP for the house pattern). node tools/invariants.mjs MUST exit 0 before you finish. No per-frame allocation in hot paths. DONE means done: work the priorities in order, ship what lands, name specific blockers. Report: file:line changes, constants, gates run VERBATIM, absolute screenshot paths, remaining items.`
const R = { type: 'object', additionalProperties: false, required: ['report'], properties: { report: { type: 'string' } } }
const E = { effort: 'high', schema: R }
phase('Build')
const [enemies, props, vfx] = await parallel([
  () => agent(`${COMMON}

YOUR LANE: src/enemies/* (Enemy.js, Enemies.js, glbBody.js, glbAnim.js, bodies.js, rig.js, defs.js, materials.js). Archived partial from the killed run: tools/out/wave6-partials/enemies-partial.patch.

THE USER SAW THE KILLED RUN'S HALF-WIRED MIXER LIVE AND SAID (verbatim): "they're all floppy and their limbs flail everywhere when they move - it's much worse than the initial ones we had before changes". THIS IS YOUR QUALITY BAR: animation is judged BY EYE with contact sheets, per creature, against the current procedural gait — a gate pass is necessary but NOT sufficient. If a baked clip looks worse than the procedural gait for a creature, that creature KEEPS PROCEDURAL and you say so.

CURRENT STATE: enemies code is at HEAD (all-procedural animation, the known state). game.assets.clips(name) returns AnimationClip[] ('idle', 'walk' or 'quadruped-walk', 'run') and clip-bearing GLBs are ALREADY in public/assets/creatures/ (11 creatures + wayfinder; serpent/giant have no clips; wisp procedural by decree). BUT: node tools/animcheck.mjs currently returns 19/23 — FAIL skyserpent, wraith, leviathan, voidhorror, all "RAILS: limbless/flying body barely deforms while travelling" (report: tools/out/animcheck/anim-report.json). The pre-swap GLBs are in git history (commit 87c494a is the swap; \`git show 87c494a~1:public/assets/creatures/wraith.glb > /tmp-file\` recovers the old one — but DO NOT commit or checkout via git; write recovered bytes to a scratch location and compare, and if a swapped GLB is genuinely the regression, say so in your report and the orchestrator will act).

JOBS IN ORDER:
1. TRIAGE the 4 animcheck failures: wraith/voidhorror share the swapped wraith.glb; skyserpent/leviathan share serpent.glb which was NOT swapped — so first determine whether this is (a) the swapped GLB deforming less under the procedural driver, (b) a pre-existing marginal RAILS number that flickers run to run, or (c) something else. Fix the real cause (thresholds are orchestrator-owned — do NOT widen them).
2. WIRE THE ANIMATION MIXER: (a) glbBody.js passes clips through; (b) Enemy.js: AnimationMixer per GLB instance, idle/walk|quadruped-walk/run cross-faded by e.speedN; (c) glbAnim.js: procedural attack/stagger/death layered AFTER mixer.update() (both write bone rotations); (d) the distance-banded animEvery rate limiter still applies (mixer stepped only on animate frames, pose held between). Creatures without clips keep the current procedural path untouched.
   THEN THE EYE PASS, per clip-bearing creature: 12-frame walking contact sheet (burst + python PIL montage, or read the burst frames) baked vs procedural. Floppy = wrong. Foot-planted, weighty, joints move like joints = right. Keep the better one per creature via a per-type opt-in table (e.g. USE_CLIPS in glbBody/Enemy) so a bad retarget cannot ship.
3. SECOND POSE RATE LIMITER (if still present after your work): riftling measured 50% held / 100% alternating at 19 m where animEvery=1 — hunt the flyer/hover path for a second throttle.
4. SERAPH RIG (celestial blocker): a walking seraph at 25 m has a limb leaving the hip as a long boneless tapering streamer — seraph uses body 'sentinel'; HANDOVER notes sentinel's limb classification is odd (g0.L / g1.R). Diagnose in rig.js/glbAnim.js, fix the deformation.
VERIFY: node tools/animcheck.mjs VERBATIM (target 23/23 — and explain any residual), contact sheets for hound + treant + wraith + one flyer, seraph walk burst at 25 m. Budget ~0.6 ms enemies CPU — respect animEvery bands.`, { label: 'enemies-redo', ...E }),

  () => agent(`${COMMON}

YOUR LANE: src/world/Props.js (you may ADD colliders through the existing Colliders registry API, additively). Archived partial from the killed run: tools/out/wave6-partials/props-partial.patch (367 lines, unreported — consult, don't trust). docs/ORNAMENT-STANDARD.md is the craft standard (flute/moulding/dentils/coffer/relief/gold-trace/weather + the 200 m / 40 m / 8 m acceptance rule). Priorities, each a named wave-5 blocker:
1. ELDERHEART (forest): "NO readable silhouette at ANY range" — the claimed 50 m pale-silver bole + 66 m crown must actually read: visible above the canopy from 200-400 m outside, distinct at 40 m, material truth at 8 m.
2. KHARAZ-DUN GATE (dragon): "the hero landmark has no doorway" (verified at 35/20/12/6 m on the arrival axis) — cut a real recessed portal with depth: door leaves or an open passage with interior darkness.
3. LOST MONOLITHS: "the gold ornament on the monolith ring is BROKEN GEOMETRY, x16" (moulded caps float/loop off each shaft) — fix the transform math; and the aether elements ("flat pastel decals") must read luminous: saturated blue-violet on real geometry, channel-capped ~1.02 (blob decree).
4. VALE: ruin the Sundered Spire plaza (cracked/heaved slabs, rubble, weathering — it is a RUIN on a pristine polished plaza today); ornament the Aetheryte hub (currently a plain brick cone with 2-poly banners).
5. SHADOWFEN: Hagstone albedo lift + carving (currently a near-black cutout, albedo ~0.02-0.05); trilithon imposts render as flat ribbons looping off the shafts — same transform-math class as the monolith caps.
6. SUNKEN: Drowned Court ornament reach + material variety (column/arch/plinth/lintel/floor all carry the same mint swirl at the same scale today).
7. CELESTIAL: dress the floating isles (bald cream domes when stood on: tops get stone/ruin fragments, undersides get rock strata + hanging crystals); Empyrean Gate podium wall 8 m material truth.
8. WAYFINDER NPC: game.assets.model('wayfinder') (15k tris, 23 joints) + game.assets.clips('wayfinder') (idle/walk/run) — replace the flat-face-slab figure in the spawn meadow: clone scene AND materials, AnimationMixer playing idle, correct scale/facing.
Work IN ORDER. Screenshot every landmark you touch at its acceptance distances. Budgets: instanced prop ≤ 32k tris, ≤350 draw calls at q=high — check stats() after your heaviest landmark.`, { label: 'props-redo', ...E }),

  () => agent(`${COMMON}

YOUR LANE: src/vfx/* (VFX.js, Brush.js, Particles.js, Extras.js, Filaments.js, Textures.js) + src/player/Abilities.js. Archived partial from the killed run: tools/out/wave6-partials/vfx-abilities-partial.patch (consult, don't trust — one of the killed partials broke boot with a stuck loading overlay + black composer output, and this lane's Particles/Textures edits are prime suspects; whatever you write must be verified BOOTING first).
JOBS:
1. COMBAT GATE TO ZERO: node tools/inspect.mjs --name cvfx-vfx3 --q high --script tools/scripts/combat-blob-steps.json --url http://127.0.0.1:5179/ (WITH lock) then python tools/combatcheck.py tools/out/cvfx-vfx3. Baseline residuals from run r4 (tools/out/cvfx-r4/combatcheck.json): celestial one 51k-px pale-cyan disc + small cores (skyserpent arc detonations over white marble), shadowfen strand-green cores 1-4k px, sunken/vale/tundra small cores. Structural guards already in HEAD (keep them all — invariants rule (o) pins them): BRUSH_MINCH_CAP, near-camera coverage fades (Particles + Combat halo), explosion overlap governor (o.dampen), energy conservation er=min(1,2.2/r), death pop hued. Finish with the same principles WITHOUT killing punch (explosions stay vivid elemental fire/arc/void). Iterate capture→check→fix; report the final verdict verbatim.
2. THE INFERNAL "GIANT FLAT BLUE POLYGON" IS YOURS: the sky lane proved it is a world-anchored VFX-class object — a TALL column of layered blue camera-facing translucent sheets with a single red-orange dot at its tip, N-NNE of the 120 m approach point (evidence: tools/out/skyw5-wedge/shot-w-upleft.png, crop-wedge.png; camera 212.4, 739.5, hour 17.6). That signature sounds like a loot beacon / quest column / weather pillar rendered at absurd scale or unfogged. Find the emitter, fix scale/fog/alpha so it reads as an intentional beacon, not a sky-filling wedge.
3. IMPACT DECAL: "a huge pure-black soft radial blob — ~200 px at 2 m, no crater, no rim, no dust... reads as a hole in the world" — rebuild: small dark core + lighter rim/fracture streaks + brief dust, honest 0.1-0.2 m sizing.
4. SUPER "STARFALL" (Abilities.js + vfx): "two enormous flat solid-yellow cartoon hands... plus a flat yellow disc, plus a screen-wide barrel warp that pulls the world's corners in and exposes violet/red garbage at the frame edges." Remove or clamp the warp (no frame-edge garbage ever); replace the flat hands/disc with shaded, energy-treated visuals on the gold/aether palette (gradients, fresnel, particles, all capped).
5. INFERNAL FIRING WEDGE: "holding LMB produces a perfectly straight, unfeathered polygon edge corner-to-corner" — tracer/beam quad bug in the muzzle path (Extras.js tracers are a suspect) — find and fix.
VERIFY each visually + combat gate verbatim + invariants + ONE full boot screenshot at the very end proving no overlay/composer breakage.`, { label: 'vfx-redo', ...E }),
])
return { enemies, props, vfx }