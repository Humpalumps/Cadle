/**
 * RPG: stats/leveling, loot + inventory, quests, dialogue/NPCs (Web Speech voice), story, save/load.
 * API: game.rpg.stats, .inventory, .quests, .dialogue, .save(), .load()
 * STUB — built after fundamentals sign off.
 */
export class RPG {
  constructor(game) { this.game = game; }
  init() {}
  update(dt, t) {}
}
